const LOGIN_USER = 'login_user';
const LOGOUT_USER = 'logout_user';

const Action = {

};

const loginUser = () => {
  return (dispatch) => {

  }
};

const reducer = (action) => {
  switch (action.type) {
    case LOGIN_USER:

      break;
  }
};